import axios from "axios";

/* const axiosInstance = axios.create({
  baseURL: "",
});

export default {
  getIp(ip) {
    return axiosInstance
      .get(`v1?apiKey=at_s=${ip}`)
      .then((response) => {
        return response.data;
      })
      .catch((error) => {
        throw error;
      });
  },
}; */
